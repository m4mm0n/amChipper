# Samples and Test Songs

The repository does not publish copied SID/NSF/tracker corpora by default.

For development, keep local corpora outside Git and open them through the app. This avoids accidentally relicensing third-party music as GPLv3 source.
